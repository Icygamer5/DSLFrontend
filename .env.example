# ============================================================
# Crisis Funding Dashboard — .env template
# Copy to .env and fill in your values. Do not commit .env.
# ============================================================

# --- Map (frontend) ---
# Get a token at https://account.mapbox.com/access-tokens/
VITE_MAPBOX_ACCESS_TOKEN=pk.your_mapbox_public_token

# --- Databricks (backend only — no VITE_ prefix) ---
# 1. PAT: Databricks → Settings → Developer → Access tokens → Generate
DATABRICKS_PAT=dapixxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# 2. Hostname: from your workspace URL, e.g. https://dbc-XXXX-XXXX.cloud.databricks.com
DATABRICKS_SERVER_HOSTNAME=dbc-20724627-a496.cloud.databricks.com

# 3. SQL Warehouse: Databricks → SQL Warehouses → copy "HTTP Path" or "ID"
#    Use Serverless recommended (e.g. "Serverless Starter Warehouse", ID: dc48d30bfbf3859c).
DATABRICKS_HTTP_PATH=/sql/1.0/warehouses/dc48d30bfbf3859c
DATABRICKS_WAREHOUSE_ID=dc48d30bfbf3859c

# 4. Table: Unity Catalog table for crises (catalog.schema.table)
#    Token needs SELECT on this table.
DATABRICKS_TOP_CRISES_TABLE=crisis.data_def.gold_crisis_impact

# 5. Genie: space ID from Genie space URL .../genie/rooms/<THIS_ID>
GENIE_SPACE_ID=01f10fc204ca12d99e5339a776057d15

# --- Optional ---
# PORT=3001
# DEBUG_GENIE=1
